syntax = "proto2";

import "tensorflow/core/example/example.proto";

package language.realm;

service Preprocessing {
  // Return a tf.Example given an unused (usually empty) input tf.Example.
  rpc PopExample(tensorflow.Example) returns (tensorflow.Example) {
  }
}
